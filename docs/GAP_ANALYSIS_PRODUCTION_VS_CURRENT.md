# 이전 결과물 vs 현재 계획: 상용화급 엣지 AI 갭 분석

**작성일**: 2025-01-09
**목적**: 이전 production 결과물과 현재 계획 간 실제 구현 수준 대조 분석

---

## 🔍 핵심 발견 (Critical Findings)

### ⚠️ **이전 production 결과물에는 상용화급 Edge AI가 구현되어 있지 않음**

**검증 결과**:
```bash
# 학습된 AI 모델 파일 검색
$ find /home/user/edgeai -name "*.pth" -o -name "*.tflite" -o -name "*.dlc" -o -name "*.onnx"
# 결과: 없음

$ ls /home/user/edgeai/models/
# 결과: 디렉토리조차 존재하지 않음

$ find /home/user/edgeai -name "train.csv" -o -name "test.csv"
# 결과: 학습 데이터셋 없음
```

---

## 📊 대조 분석표

| 구분 | 이전 Production | 현재 edgeai 프로젝트 | 상용화급 요구사항 | 갭 분석 |
|------|----------------|---------------------|------------------|---------|
| **AI 프레임워크** | ❌ **Cloud AI (Gemini)** | ✅ Edge AI 설계 (PyTorch) | ✅ On-device Edge AI | **이전: Cloud 의존, 현재: 미학습** |
| **학습된 모델** | ❌ 없음 (Cloud API 사용) | ❌ 없음 (학습 전) | ✅ .tflite/.dlc 필수 | **둘 다 없음** |
| **모델 크기** | N/A (Cloud) | ❌ 미측정 | ✅ <14MB | **검증 불가** |
| **추론 지연** | ~500ms (Cloud API) | ❌ 미측정 | ✅ <50ms (P95) | **500ms vs 목표 50ms** |
| **오프라인 작동** | ❌ 불가능 (Cloud 필수) | ✅ 설계됨 | ✅ 필수 | **이전: 불가능** |
| **데이터셋** | ❌ 없음 | ✅ 합성 생성기 | ✅ 35,000+ samples | **현재: 생성기만** |
| **모델 학습** | ❌ 미실행 | ❌ 미실행 | ✅ 학습 완료 필수 | **둘 다 미실행** |
| **양자화** | N/A | ❌ 미실행 | ✅ INT8 필수 | **미구현** |
| **안드로이드 추론** | ❌ 없음 | ❌ 없음 (설계만) | ✅ TFLite/SNPE | **둘 다 없음** |
| **성능 검증** | ❌ 없음 | ❌ 없음 | ✅ 벤치마크 필수 | **미검증** |

---

## 📋 상세 갭 분석

### 1. AI 모델 구현 상태

#### 이전 Production (glec-dtg-ai-production)

**실제 구현**:
```python
# Cloud AI 기반 (Vertex AI Gemini Fine-tuned)
# 로컬 Edge AI 모델 없음

[CAN 데이터] → [MQTT] → [Cloud Vertex AI] → [Gemini Fine-tuned] → [결과 반환]
                                                  ↓
                                          (500ms+ 지연)
                                          (인터넷 필수)
                                          (API 비용)
```

**장점**:
- ✅ 고급 자연어 분석 (Gemini LLM)
- ✅ 지속적 학습 가능
- ✅ 복잡한 패턴 인식

**단점**:
- ❌ **Edge AI 아님** (Cloud 의존)
- ❌ **오프라인 불가능**
- ❌ **500ms+ 지연** (상용차 실시간 요구사항 미충족)
- ❌ **API 비용** (차량당 월 $10-50)
- ❌ **데이터 프라이버시** (클라우드 전송)

---

#### 현재 edgeai 프로젝트

**실제 구현**:
```bash
# 학습 스크립트만 존재, 학습된 모델 없음

ai-models/
├── training/
│   ├── train_tcn.py          ✅ 스크립트 존재
│   ├── train_lstm_ae.py      ✅ 스크립트 존재
│   └── train_lightgbm.py     ✅ 스크립트 존재
├── optimization/
│   └── quantize_model.py     ✅ 스크립트 존재
├── conversion/
│   └── export_onnx.py        ✅ 스크립트 존재
└── tests/
    ├── test_tcn.py           ✅ 테스트 존재
    ├── test_lstm_ae.py       ✅ 테스트 존재
    └── test_lightgbm.py      ✅ 테스트 존재

# 그러나...
models/                        ❌ 디렉토리 없음
  ├── tcn_trained.pth          ❌ 없음
  ├── lstm_ae_trained.pth      ❌ 없음
  ├── lightgbm_classifier.txt  ❌ 없음
  ├── tcn_quantized.tflite     ❌ 없음
  └── tcn_quantized.dlc        ❌ 없음

datasets/                      ❌ 디렉토리 없음
  ├── train.csv                ❌ 없음
  ├── val.csv                  ❌ 없음
  └── test.csv                 ❌ 없음
```

**장점**:
- ✅ Edge AI 아키텍처 설계 완료
- ✅ 100% 오픈소스 (PyTorch, LightGBM, TFLite)
- ✅ 합성 데이터 생성기 준비
- ✅ 테스트 케이스 61+ 준비

**단점**:
- ❌ **학습된 모델 없음**
- ❌ **학습 데이터셋 없음** (생성기만 있음)
- ❌ **성능 검증 불가능** (모델 없음)
- ❌ **안드로이드 통합 불가능** (모델 없음)

---

### 2. 상용화급 Edge AI 요구사항

#### 필수 구성 요소

```
✅ = 완료, ⏸️ = 부분 완료, ❌ = 미구현

┌─────────────────────────────────────────────────────────────┐
│ 1. 학습 데이터셋 (35,000+ samples)                         │
│    ├─ 이전 production: ❌ 없음                              │
│    ├─ 현재 edgeai: ⏸️ 합성 생성기만 (미실행)              │
│    └─ 요구사항: ✅ 생성 필요                                │
├─────────────────────────────────────────────────────────────┤
│ 2. AI 모델 학습 (TCN, LSTM-AE, LightGBM)                   │
│    ├─ 이전 production: ❌ 없음 (Cloud AI 사용)              │
│    ├─ 현재 edgeai: ❌ 미실행 (스크립트만)                  │
│    └─ 요구사항: ✅ GPU 환경에서 학습 필요                   │
├─────────────────────────────────────────────────────────────┤
│ 3. 모델 검증 (Accuracy, F1-score, R²)                      │
│    ├─ 이전 production: ❌ 없음                              │
│    ├─ 현재 edgeai: ❌ 미실행                               │
│    └─ 요구사항: ✅ TCN R²>0.85, LSTM F1>0.85, LGB Acc>0.90 │
├─────────────────────────────────────────────────────────────┤
│ 4. 모델 최적화 (INT8 양자화)                                │
│    ├─ 이전 production: ❌ 없음                              │
│    ├─ 현재 edgeai: ❌ 미실행 (스크립트만)                  │
│    └─ 요구사항: ✅ 크기 <14MB, 정확도 하락 <5%              │
├─────────────────────────────────────────────────────────────┤
│ 5. 모델 변환 (PyTorch → ONNX → TFLite/SNPE)                │
│    ├─ 이전 production: ❌ 없음                              │
│    ├─ 현재 edgeai: ❌ 미실행                               │
│    └─ 요구사항: ✅ .tflite, .dlc 파일 필요                  │
├─────────────────────────────────────────────────────────────┤
│ 6. 안드로이드 추론 엔진 (TFLite/SNPE)                       │
│    ├─ 이전 production: ❌ 없음 (Cloud API만)                │
│    ├─ 현재 edgeai: ⏸️ 설계만 (코드 없음)                  │
│    └─ 요구사항: ✅ Kotlin 추론 서비스 구현                  │
├─────────────────────────────────────────────────────────────┤
│ 7. 성능 벤치마크 (<50ms, <100MB 메모리, <2W)               │
│    ├─ 이전 production: ❌ 없음 (Cloud 500ms+)               │
│    ├─ 현재 edgeai: ❌ 미실행                               │
│    └─ 요구사항: ✅ Snapdragon 실기기 측정 필수              │
├─────────────────────────────────────────────────────────────┤
│ 8. 하드웨어 가속 (Qualcomm DSP/HTP)                         │
│    ├─ 이전 production: ❌ 없음                              │
│    ├─ 현재 edgeai: ❌ 미구현                               │
│    └─ 요구사항: ✅ SNPE Runtime 필수                        │
├─────────────────────────────────────────────────────────────┤
│ 9. End-to-End 검증 (CAN → 추론 → 결과)                     │
│    ├─ 이전 production: ⏸️ 부분 (CAN → Cloud)              │
│    ├─ 현재 edgeai: ❌ 미구현                               │
│    └─ 요구사항: ✅ 실차 테스트 필수                         │
└─────────────────────────────────────────────────────────────┘
```

---

### 3. 비교 시나리오: 급제동 탐지

#### 시나리오: 차량이 -6 m/s² 급제동 발생

**이전 Production (Cloud AI)**:
```
Time 0ms:   [CAN 센서] → 급제동 감지 (-6 m/s²)
Time 50ms:  [Android] → MQTT 큐에 추가
Time 150ms: [MQTT] → Cloud Vertex AI 전송
Time 250ms: [Cloud] → Gemini 분석 시작
Time 650ms: [Cloud] → 분석 완료 "위험 운전 감지"
Time 750ms: [Android] → 결과 수신
Time 800ms: [UI] → 알림 표시 "급제동 감지됨"

총 지연: 800ms (0.8초)
문제점:
- ❌ 이미 사고 발생 가능 (0.8초는 너무 김)
- ❌ 오프라인 시 작동 불가
- ❌ API 호출 비용 발생
```

**현재 edgeai 계획 (Edge AI)** - 학습 후:
```
Time 0ms:   [CAN 센서] → 급제동 감지 (-6 m/s²)
Time 5ms:   [60초 버퍼] → LSTM-AE 추론 실행
Time 35ms:  [LSTM-AE] → 이상 탐지 완료 (재구성 오차 > 0.8)
Time 40ms:  [LightGBM] → 행동 분류 "Aggressive"
Time 50ms:  [UI] → 실시간 알림 "급제동 감지됨"

총 지연: 50ms (0.05초)
장점:
- ✅ 16배 빠름 (800ms → 50ms)
- ✅ 오프라인 작동
- ✅ API 비용 없음
- ✅ 데이터 프라이버시 보장

그러나 현재 상태:
- ❌ 모델이 없어서 실행 불가능
```

---

### 4. 통합 전략의 현실

#### 문서상 계획 (docs/INTEGRATION_ANALYSIS.md)

```
"하이브리드 아키텍처"

Edge: TCN/LSTM-AE/LightGBM (50ms)
  ↓ 60초마다
Cloud: Gemini Fine-tuned (고급 분석)
```

#### 실제 구현 가능성

| 컴포넌트 | 상태 | 구현 난이도 | 예상 시간 |
|---------|------|------------|----------|
| **Edge 부분** |
| ├─ 데이터 생성 | ⏸️ 생성기만 | ⭐ 쉬움 | 2시간 |
| ├─ 모델 학습 | ❌ 미실행 | ⭐⭐⭐ 중간 | 12시간 (GPU 필요) |
| ├─ 모델 양자화 | ❌ 미실행 | ⭐⭐ 쉬움 | 3시간 |
| ├─ 안드로이드 통합 | ❌ 미구현 | ⭐⭐⭐⭐ 어려움 | 16시간 |
| └─ 성능 최적화 | ❌ 미구현 | ⭐⭐⭐⭐ 어려움 | 8시간 |
| **Cloud 부분** |
| ├─ Vertex AI 연결 | ❌ 미구현 | ⭐⭐⭐ 중간 | 8시간 |
| ├─ Edge-Cloud 동기화 | ❌ 미구현 | ⭐⭐⭐ 중간 | 6시간 |
| └─ 하이브리드 의사결정 | ❌ 미구현 | ⭐⭐⭐⭐ 어려움 | 10시간 |

**총 예상 시간**: 65시간 (약 8-10 작업일)

---

## 🎯 상용화까지 필요한 작업

### Phase 4: Edge AI 모델 개발 (필수)

#### 4-A. 데이터 생성 (2시간, CPU)
```bash
cd data-generation
python synthetic_driving_simulator.py --output-dir ../datasets --samples 35000

# 출력:
# ✅ datasets/train.csv (28,000 samples)
# ✅ datasets/val.csv (3,500 samples)
# ✅ datasets/test.csv (3,500 samples)
```

**상태**: ✅ 준비 완료 (웹 환경 가능)

---

#### 4-B. 모델 학습 (12시간, GPU 필요)
```bash
# 로컬 환경 필요: RTX 3080/3090 + CUDA 11.8+

# TCN 학습 (6시간)
python ai-models/training/train_tcn.py \
    --data datasets/train.csv \
    --epochs 100 \
    --batch-size 64 \
    --gpu 0

# LSTM-AE 학습 (4시간)
python ai-models/training/train_lstm_ae.py \
    --data datasets/train.csv \
    --epochs 50 \
    --batch-size 64 \
    --gpu 0

# LightGBM 학습 (2시간, CPU)
python ai-models/training/train_lightgbm.py \
    --data datasets/train.csv \
    --num-boost-round 200
```

**출력**:
- ✅ `models/tcn_trained.pth` (15-20MB)
- ✅ `models/lstm_ae_trained.pth` (12-18MB)
- ✅ `models/lightgbm_classifier.txt` (8-12MB)

**검증 기준**:
- TCN R² > 0.85
- LSTM-AE F1 > 0.85
- LightGBM Accuracy > 0.90

**상태**: ❌ 미실행 (GPU 로컬 환경 필요)

---

#### 4-C. 모델 최적화 (3시간, CPU)
```bash
# PyTorch → ONNX
python ai-models/conversion/export_onnx.py \
    --model tcn \
    --input models/tcn_trained.pth \
    --output models/tcn.onnx

# ONNX → TFLite (INT8 양자화)
python ai-models/optimization/quantize_model.py \
    --model tcn \
    --input models/tcn.onnx \
    --output models/tcn_quantized.tflite \
    --calibration-data datasets/val.csv
```

**출력**:
- ✅ `models/tcn_quantized.tflite` (2-4MB, <5% 정확도 하락)
- ✅ `models/lstm_ae_quantized.tflite` (2-3MB)
- ✅ `models/lightgbm_classifier.txt` (그대로 사용)

**상태**: ❌ 미실행 (학습 모델 필요)

---

#### 4-D. 안드로이드 추론 통합 (16시간, 로컬)

**필요 작업**:

1. **TFLite 추론 엔진** (6시간)
```kotlin
// android-dtg/app/src/main/java/com/glec/dtg/inference/TFLiteInferenceEngine.kt

class TFLiteInferenceEngine(context: Context) {
    private val interpreter: Interpreter

    init {
        val options = Interpreter.Options().apply {
            setUseNNAPI(true)  // Android Neural Networks API
            addDelegate(GpuDelegate())  // GPU 가속
            setNumThreads(4)
        }

        val modelFile = loadModelFile("tcn_quantized.tflite")
        interpreter = Interpreter(modelFile, options)
    }

    fun predict(canData: FloatArray): Float {
        val input = preprocessCANData(canData)
        val output = FloatArray(1)
        interpreter.run(input, output)
        return output[0]
    }
}
```

2. **SNPE 추론 엔진** (8시간)
```kotlin
// android-dtg/app/src/main/java/com/glec/dtg/inference/SNPEInferenceEngine.kt

class SNPEInferenceEngine(context: Context) {
    private val snpe: SNPE

    init {
        val runtime = NeuralNetwork.Runtime.DSP  // Qualcomm DSP
        snpe = SNPE.NeuralNetworkBuilder(context)
            .setModel(loadModelFile("tcn_quantized.dlc"))
            .setRuntimeOrder(runtime)
            .setPerformanceProfile(HIGH_PERFORMANCE)
            .build()
    }

    fun predict(canData: FloatArray): Float {
        val output = snpe.execute(mapOf("input" to canData))
        return output["output"]!![0]
    }
}
```

3. **추론 서비스** (2시간)
```kotlin
// android-dtg/app/src/main/java/com/glec/dtg/service/EdgeAIInferenceService.kt

class EdgeAIInferenceService : Service() {
    private val tcnEngine = TFLiteInferenceEngine(this)
    private val lstmEngine = TFLiteInferenceEngine(this)
    private val lgbmEngine = LightGBMInferenceEngine(this)

    fun processCANData(canData: CANData) {
        // 60초 버퍼 관리
        canDataBuffer.add(canData)

        if (canDataBuffer.isFull()) {
            // 병렬 추론
            val results = runBlocking {
                val fuel = async { tcnEngine.predict(canDataBuffer.toArray()) }
                val anomaly = async { lstmEngine.detectAnomaly(canDataBuffer.toArray()) }
                val behavior = async { lgbmEngine.classify(extractFeatures()) }

                AIResult(fuel.await(), anomaly.await(), behavior.await())
            }

            handleResults(results)
        }
    }
}
```

**상태**: ❌ 미구현 (모델 파일 필요)

---

### Phase 5: 성능 검증 (8시간, 실기기 필요)

```bash
# Snapdragon 865 디바이스에서 실행

# 1. 지연 시간 측정
adb shell am instrument -w \
    -e class com.glec.dtg.InferenceLatencyTest \
    com.glec.dtg.test/androidx.test.runner.AndroidJUnitRunner

# 목표: P95 < 50ms

# 2. 메모리 사용량
adb shell dumpsys meminfo com.glec.dtg

# 목표: Peak < 100MB

# 3. 배터리 소비
# Android Profiler로 측정

# 목표: Average < 2W
```

**상태**: ❌ 미실행 (안드로이드 앱 필요)

---

## 📈 완성도 비교

### 이전 Production (glec-dtg-ai-production)

```
전체 완성도: 40%

인프라         ████████████████████ 100% (MQTT, Cloud, 파이프라인)
UI/UX          ████████████████     80%  (3D 대시보드, 음성 AI)
Edge AI        ░░░░░░░░░░░░░░░░░░░░ 0%   (없음, Cloud AI만)
오프라인 작동   ░░░░░░░░░░░░░░░░░░░░ 0%   (불가능)
실시간 응답     ████                 20%  (500ms, 목표 50ms)
```

### 현재 edgeai 프로젝트

```
전체 완성도: 55%

인프라         ████████████         60%  (CAN, UART, BLE 설계)
UI/UX          ██████               30%  (기본 UI만)
Edge AI 설계   ████████████████████ 100% (아키텍처 완료)
Edge AI 구현   ░░░░░░░░░░░░░░░░░░░░ 0%   (모델 미학습)
오프라인 작동   ████████████████████ 100% (설계 완료)
실시간 응답     ████████████████████ 100% (설계 목표 50ms)
```

### 상용화급 요구사항

```
목표 완성도: 100%

인프라         ████████████████████ 100%
UI/UX          ████████████████████ 100%
Edge AI 설계   ████████████████████ 100%
Edge AI 구현   ████████████████████ 100% ⬅️ 필수!
오프라인 작동   ████████████████████ 100%
실시간 응답     ████████████████████ 100%
성능 검증      ████████████████████ 100% ⬅️ 필수!
상용 배포      ████████████████████ 100%
```

---

## 🚧 현재 블로커 (Blockers)

### 1. 학습된 AI 모델 없음 ⛔
- **문제**: TCN, LSTM-AE, LightGBM 모두 미학습
- **영향**: 안드로이드 앱에서 추론 불가능
- **해결**: GPU 환경에서 12시간 학습 필요
- **우선순위**: 🔴 **최고**

### 2. 학습 데이터셋 없음 ⛔
- **문제**: datasets/ 디렉토리 없음
- **영향**: 모델 학습 불가능
- **해결**: 합성 생성기 실행 (2시간, CPU 가능)
- **우선순위**: 🔴 **최고**

### 3. 안드로이드 추론 엔진 없음 ⛔
- **문제**: TFLite/SNPE 통합 코드 없음
- **영향**: 모델이 있어도 사용 불가능
- **해결**: Kotlin 추론 서비스 구현 (16시간)
- **우선순위**: 🟡 **높음** (모델 학습 후)

### 4. 성능 검증 불가능 ⛔
- **문제**: 실기기 벤치마크 미실행
- **영향**: 50ms 목표 달성 여부 불명
- **해결**: Snapdragon 디바이스 측정 (8시간)
- **우선순위**: 🟢 **중간** (통합 후)

---

## 🎯 상용화 로드맵

### Week 1-2: 데이터 및 모델 개발

| Day | 작업 | 환경 | 시간 | 상태 |
|-----|------|------|------|------|
| 1 | 합성 데이터 생성 (35,000 samples) | Web/Local CPU | 2h | ⏸️ |
| 2-3 | TCN 학습 + 검증 (R² > 0.85) | Local GPU | 6h | ❌ |
| 4-5 | LSTM-AE 학습 + 검증 (F1 > 0.85) | Local GPU | 4h | ❌ |
| 6 | LightGBM 학습 + 검증 (Acc > 0.90) | Local CPU | 2h | ❌ |
| 7 | 모델 양자화 (INT8, 크기 검증) | Local CPU | 3h | ❌ |

### Week 3-4: 안드로이드 통합

| Day | 작업 | 환경 | 시간 | 상태 |
|-----|------|------|------|------|
| 8-9 | TFLite 추론 엔진 구현 | Local Android Studio | 6h | ❌ |
| 10-11 | SNPE 추론 엔진 구현 | Local Android Studio | 8h | ❌ |
| 12 | LightGBM Java 통합 | Local Android Studio | 2h | ❌ |
| 13-14 | 추론 서비스 구현 (60초 버퍼) | Local Android Studio | 6h | ❌ |

### Week 5: 성능 최적화 및 검증

| Day | 작업 | 환경 | 시간 | 상태 |
|-----|------|------|------|------|
| 15-16 | 지연 시간 측정 및 최적화 | Snapdragon 865 | 8h | ❌ |
| 17 | 메모리/배터리 프로파일링 | Snapdragon 865 | 4h | ❌ |
| 18-19 | 실차 E2E 테스트 | 실제 차량 | 8h | ❌ |
| 20 | 문서화 및 배포 준비 | Local | 4h | ❌ |

**총 소요 시간**: 63시간 (약 20 작업일)

---

## 📊 결론

### 핵심 발견

1. ⚠️ **이전 production은 Cloud AI**: Edge AI 아님, 오프라인 불가, 500ms+ 지연
2. ⚠️ **현재 edgeai는 설계만**: 학습된 모델 없음, 데이터셋 없음, 안드로이드 통합 없음
3. ✅ **현재가 더 적합**: 설계는 우수 (Edge AI, 오프라인, 50ms 목표)
4. ❌ **둘 다 상용화급 아님**: 실제 작동하는 Edge AI 모델 없음

### 우선순위 작업

| 순위 | 작업 | 예상 시간 | 필수 환경 | 중요도 |
|------|------|----------|----------|--------|
| 1 | **데이터 생성** | 2시간 | Web/CPU | 🔴 |
| 2 | **모델 학습** | 12시간 | GPU (RTX 3080+) | 🔴 |
| 3 | **모델 양자화** | 3시간 | CPU | 🔴 |
| 4 | **안드로이드 추론 통합** | 16시간 | Android Studio | 🟡 |
| 5 | **성능 벤치마크** | 8시간 | Snapdragon 실기기 | 🟢 |

### 권장 사항

1. **즉시 실행 가능** (웹 환경):
   ```bash
   # 합성 데이터 생성 (2시간)
   cd data-generation
   python synthetic_driving_simulator.py --output-dir ../datasets --samples 35000
   ```

2. **로컬 GPU 환경에서 실행**:
   ```bash
   # 모델 학습 (12시간)
   python ai-models/training/train_tcn.py --data datasets/train.csv
   python ai-models/training/train_lstm_ae.py --data datasets/train.csv
   python ai-models/training/train_lightgbm.py --data datasets/train.csv
   ```

3. **로컬 Android Studio에서 실행**:
   - TFLite/SNPE 추론 엔진 구현
   - 추론 서비스 통합
   - 성능 벤치마크

**예상 상용화 시점**: 현재 + 63시간 (약 3주, 로컬 환경 가정)

---

**Generated**: 2025-01-09
**Branch**: `claude/artifact-701ca010-011CUxNEi8V3zxgnuGp9E8Ss`
**Status**: Gap Analysis Complete
